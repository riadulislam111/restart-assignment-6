//1 : Undefined মানে variable declared   করা হয়েছে কিন্তু কোন ভ্যালু দেওয়া হয়নাই,  আর Null হলো একটা ভ্যালু জা একজন devoloper নিজে বসিয়ে দেয়

//2: Map() arry নিয়ে কাজ করে,  নতুন arry return করে। 
আর forEach কোন উপাদান নিয়ে কাজ করে ফেরত দেই

//3: == সুধু value চেক করে আর === value or type দুইটি চেক করে

//4: Async or await ডাটা fetch করার সময় একটা গুরুত্বপূর্ণ ভুমিকা রাখে, code গুলা synchronous ভাবে কাজ করায়, code এর error handle করে

//5: Global Scope দিয়ে যে কোন জাইগা থেকে access করা জায়। 

function এর বাইরে ভিতরে সব জাইগা থেকে access করা জায়। 

function scope সুধু মাত্র function এর ভিতরের থেকে access পাউয়া জায়।

block scope হলো variable এর ভিতরে থাকে, variable যেমন let, const এর ভিতরে block থাকে।  
অন্য জায়গা থেকে access পাওয়া জায় নাহ।

